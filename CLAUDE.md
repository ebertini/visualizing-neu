# CLAUDE.md

Guidance for working in this repository. Read this before touching data, notebooks, or the pipeline.

## What this project is

An exploratory **data-visualization project on Northeastern University's faculty funding and grant history** (window ~1995–2026, ~$2.18B in awards). The deliverable is an interactive visualization telling the story of NEU grant funding. Current work is EDA + topic modeling in Jupyter notebooks published to GitHub Pages. A dual-mode **Plotly Dash** dashboard (touchscreen **kiosk** build + responsive public browser build from one codebase) is still a possible final goal, but that direction is under-defined and `docs/WEEKLY_PLAN.md`, which used to describe it, is now deprecated.

**Two parallel visualization tracks publish into `docs/onlineoutput/` — know which is whose before editing either.** `docs/EnricoVis/` (`grant_atlas`, `topic_islands`, `topic_hierarchy`) is a **parallel effort by the PI** — read-only reference / inspiration, not something to casually modify. `docs/TopicVizPrototypes/` (`topic_flow`, `what_we_can_see`) is **the user's own** topic-model visualization work, built to reinforce their own analysis — it reuses EnricoVis's canonical BERTopic output as a read-only upstream input (see `src/build_viz_aggregates.py`) but writes only into its own directory. Both are "best current sense of what the final deliverable could look like" — EnricoVis for the spatial-embedding forms, TopicVizPrototypes for time/coverage forms EnricoVis doesn't cover.

Sanity-check names (known-good faculty for spot-checking joins): **Saiph Savage** (resolves correctly to Khoury College), **Michael Ann DeVito**, **Benjamin Gyori** — the latter two are not matched as a PI on any grant in the current corpus, so PI-keyed joins legitimately come up empty for them (not a bug). ID-reconciliation running example: **Chris Martens** (`faculty_id 2963712`).

## Repository layout

```
DataSet/            Raw .xlsx / .csv inputs (see caveat below — these ARE committed)
src/                ETL pipeline (build_dataset.py) + SPECTER2 embedder
data/processed/     Pipeline output: *.parquet (+ shareable *.csv) — GITIGNORED
data/nih_nsf_backfill/  NIH RePORTER / NSF Award Search backfill outputs — TRACKED (deliberate
                    .gitignore exception; see Conventions & gotchas)
notebooks/          Numbered EDA notebooks 01–08, run in order
scripts/            generate_index.py (docs) + underscore-prefixed ad-hoc diagnostics
docs/               Reference docs (*.md), published HTML, EnricoVis/ (PI's parallel work) +
                    TopicVizPrototypes/ (user's own prototypes) + NedaNotebooks/
outputs/            Generated PNG/CSV figures (w4_/w5_/w6_/w7_ prefixes) — GITIGNORED
figures/            Duplicate of some topic figures
```

## Setup & core commands

```bash
pip install -r requirements.txt          # Python 3.11+; CPU-only for everything
python src/build_dataset.py              # ~30s; regenerates ALL data/processed/*.parquet
python src/build_specter2_embeddings.py  # one-shot SPECTER2 cache for notebook 07 (~5–8 min CPU)
jupyter lab notebooks/01_schema_overview.ipynb   # run notebooks 01→08 in order

# TopicVizPrototypes' lighter build path (pandas/pyarrow/openpyxl/rapidfuzz only,
# not the full torch/bertopic stack): requirements-viz.txt + a venv.
# Bare `python3.11 -m venv` FAILS on this machine (uv-managed Python is a
# relocatable build that needs uv's own wiring — see CLAUDE.md history /
# session logs for the exact error). Use uv instead:
uv venv --python 3.11 .venv && uv pip install --python .venv/bin/python -r requirements-viz.txt
python -m src.refresh_topicviz           # build_viz_data (if stale) + build_viz_aggregates +
                                          # _check_topicviz --data-only, in one command (~1s)
python -m http.server 8000 --directory docs/TopicVizPrototypes   # the three prototype pages
                                          # fetch() their JSON — they need HTTP, NOT file://
```

Full topic-model refit pipeline (heavy deps, local-only — see `docs/TOPIC_MODEL_REFIT_CHECKLIST.md`
for the complete swap-in contract): `build_dataset.py` → `reconcile_orphans.py` →
`build_specter2_embeddings.py` → `topics_bertopic.py` → `refresh_topicviz.py`.

Abstract backfill (`src/backfill_nih_reporter.py`, `src/backfill_nsf_awards.py`) is a **separate,
rarer, rate-limited step** — run it standalone (network + `--offline`/`--limit` flags, see the
scripts' own docstrings) to (re)populate `data/nih_nsf_backfill/` (tracked in git). `build_dataset.py`
then adopts whatever's there automatically, gap-fill only, on every normal run — no separate step
needed once the backfill data exists.

`build_dataset.py` flags (both optional): `--input-dir DataSet --output-dir data/processed`.

Publishing (GitHub Pages via `.github/workflows/deploy-notebooks.yml`): on push to `main`, notebooks are `nbconvert`ed to HTML into `docs/onlineoutput/`, `scripts/generate_index.py` rebuilds the index there, and `docs/onlineoutput/` (not `docs/` itself) deploys. To do it locally: `python -m nbconvert --to html notebooks/*.ipynb --output-dir=docs/onlineoutput`.

## The data pipeline (`src/build_dataset.py`)

Reads 6 raw files from `DataSet/`, cleans/joins, and writes 7 tables to `data/processed/` (each as `.parquet` snappy **and** `.csv` utf-8-sig for Excel), plus `PIPELINE_VALIDATION.txt` (authoritative row counts + coverage). Run it whenever raw data or logic changes; notebooks auto-pick up the new parquets.

**Canonical output tables** (see `src/README.md` for full column dictionary):
| Table | Rows | Grain |
|---|---|---|
| `faculty.parquet` | 2,247 | one per faculty (2,232 HR roster + 15 `UnmatchedFaculty.csv` supplement) |
| `grants.parquet` | 2,676 | one per `grant_id`; abstract text merged in |
| `faculty_grants.parquet` | 3,144 | one per (faculty, grant) link; the join table |
| `grant_orphaned_abstracts.parquet` | 5,095 | abstracts not matched to any NEU grant |
| `faculty_missing_metadata.parquet` | 13 | grant-active faculty absent from HR (backfill worklist) |
| `faculty_id_lookup.parquet` | 2,247 | faculty_id → college/dept/AAUID |
| `personid_to_faculty.parquet` | 1,042 | the ID bridge (see below) |

**Key logic:**
- Column headers normalized via `_lower_cols`; academic ranks collapsed to ~8 buckets via `_normalize_rank`.
- Faculty **names come from grant tables** (mode/most-common `personname` per id), **not** from HR.
- AAD federal-coverage merged onto grants by **fuzzy agency-name match** (`rapidfuzz token_set_ratio`, **threshold 85**).
- Abstracts matched to grants on `sourceactivityid == grant_id`, keeping the **most-recently-updated** record per grant; non-matches become orphans.
- `personid_to_faculty` uses a **strict 100% co-occurrence majority vote** (a personid resolves only if exactly one faculty shares 100% of its grants); ties/zero → unresolved.

## Identifiers — read this, it's the #1 source of bugs

- **Canonical keys: `faculty_id` and `grant_id`.**
  - `faculty_id` = HR `Employee ID` = `ClientFacultyId` in grant tables.
  - `grant_id` = `grantid` in grant tables = `sourceactivityid` in the abstract table.
- **`"00000"`** is the reserved sentinel for grant rows whose `ClientFacultyId` was missing (currently resolves to 0 rows after cleaning, but the code path exists).
- **The `PersonId` trap:** two raw files have a `PersonId` column that are *different ID spaces with zero overlap* — `grants-with-coPI.PersonId` is the **AAUID** (Academic Analytics vendor id), `grants-with-abstract.PersonId` is an **internal upload-system id**. Neither joins directly to `faculty_id`. To go from an abstract `personid` to a faculty, use `personid_to_faculty.parquet` only.
- **AAUID** is preserved on `faculty_id_lookup` for future enrichment but is **never a join key**.

## Analytical caveats — must be disclosed in any deliverable

1. **`$2.18B headline ≠ money NEU raised.** Grants get attributed to a faculty member even if the award predates their NEU hire (source marks everyone "Northeastern"). Use **`faculty_grants.neu_status == 'earned_at_neu'`** for external/NEU-work reporting:
   - `earned_at_neu` (start ≥ hire): 2,098 rows / ~$1,408M
   - `prior_institution` (start < hire): 866 rows / ~$685M — does NOT count as NEU work
   - `unknown` (missing dates): 180 rows / ~$153M
2. **Funding-credit model matters.** PI-only vs full-credit vs fractional split materially reorder faculty leaderboards — always state which model a chart uses. (`src/README.md` has the three canonical snippets.)
3. **Data is NSF/NIH-skewed** (~88% of dollars). Internal/foundation/industry funding is largely invisible.
4. **Abstract coverage:** **2,390 of 2,676 grants (89%)** have usable abstract text as of the 2026-08-20 NIH RePORTER + NSF Award Search backfill (`src/backfill_nih_reporter.py`, `src/backfill_nsf_awards.py`, adopted gap-fill-only via `build_dataset.py`'s `_apply_abstract_backfill`) — up from 1,928 (72%) beforehand; only ~37% of the 8,075 abstract rows match a grant_id (unaffected by the backfill, a separate internal-upload-system stat). The **NIH abstract "cliff"** (near-0% coverage from 2020+), previously a **data-collection artifact, not a funding decline** that this caveat used to say only an NIH RePORTER backfill could fix — **is now closed**: 2020–2025 NIH/NIH-SubAward coverage runs 94–100%, live-verified. 5 grants (`abstract_source == 'nih_reporter_parent'`, a subaward's borrowed parent-center text) are excluded from the topic-model fit despite having stored, displayed text — see `src.clean_text.LOW_TRUST_ABSTRACT_SOURCES`. Missing the abstract still barely determines BERTopic assignment confidence, though the exact numbers moved and the comparison is now murkier: unassigned rate is **25.3%** for grants with an abstract vs **14.8%** for title-only grants (previously 28.0%/27.6%) — but this reversal is mostly a **composition effect**, not new evidence that titles got more informative: the remaining 286 title-only grants are now ~83% ONR/ARO/NASA/AFRO/DOE/NOAA, a far more homogeneous, easily-clustered population than the old, broader 740-grant title-only mix. See `notebooks/08_abstract_recovery_and_refit.ipynb`.
5. **"Unassigned" (no confident topic) — as of the 2026-08-29 keyword-classifier redo, this dropped from the single largest topic bucket by dollars to a small one.** Under the retired BERTopic fit it was **697 grants / $583M / 26.7%** of dollars (646 HDBSCAN noise + 51 in the flagged topic-14 artifact). Under the now-canonical curated keyword classifier it's **66 grants / $40.4M / 1.9%** of dollars (38 `no_keyword_evidence` — real text, zero curated terms fired, a genuine curation-coverage gap — + 28 `placeholder_title_only`, the same ONR "Grant"-titled records as before). Any topic-based chart must still show it, not drop it — it's just no longer the dominant bucket. See "Topic modeling — state of play" below.
6. Prefer `startdate`/`startdateyear` over `awarddate` (98% null). Use `totaldollars` from `ri_matches`, not the abstract file's unreliable `Dollar Amount`.
7. **External collaborators are invisible.** Every grant record is stamped "Northeastern" regardless of who else worked on it — there's no way to see co-investigators at other institutions, so any collaboration-network analysis from this data is NEU-internal only.

## Notebooks (`notebooks/`, run 01→08 in order)

All share a bootstrap: `warnings.filterwarnings('ignore')`, walk up parents to find `data/processed` as `REPO_ROOT`, load the parquets, `sns.set_theme(style='whitegrid', palette='muted')`. Figures saved `dpi=150, bbox_inches='tight'` to `outputs/` (and topic figures also to `notebooks/figures/`).

| # | Notebook | Covers |
|---|---|---|
| 01 | schema_overview | Load/profile all tables, nulls/schemas, join sanity checks, abstract-coverage diagnostics |
| 02 | funding_landscape | Baseline distributions: grant size (linear+log), duration, per-year, agency, rank/tenure, co-PI rate |
| 03 | funding_over_time | Annual/cumulative funding 2000–2025, rolling avg, college×year heatmap, agency mix, pre/post-COVID |
| 04 | who_gets_funded | Concentration (**Gini ≈ 0.632**, top-10 ≈19.6%), top-25 faculty, top-15 depts, agency×dept, `neu_status` attribution |
| 05 | collaboration_network | Co-PI NetworkX graph, degree/PageRank/betweenness, Louvain communities, cross-college matrix |
| 06 | research_topics | **LDA k=8** over abstracts — now historical/legacy, kept standalone for comparison, no longer feeds nb07 or EnricoVis; coherence + confidence validation; topics × time/college/agency/funding. Writes `outputs/topic_assignments.csv` |
| 07 | topic_deep_dive | **BERTopic** — as of the 2026-08-29 redo this is historical/comparison-only, not the pipeline's canonical topic source (see "Topic modeling — state of play"); still loads `topic_assignments.parquet` (32 topics + "Unassigned" noise cluster) independently of nb06 for its own analysis. College profiles, 8-parent-theme hierarchy, SPECTER2-centroid dendrogram, UMAP on canonical SPECTER2 coords. Writes interactive HTML (`docs/07_grant_projection_specter2.html` — deprecated/unpublished, see reference map) |
| 08 | abstract_recovery_and_refit | Light-deps-only report (no `bertopic`/`torch`/`umap`) on the NIH RePORTER + NSF Award Search abstract backfill and the BERTopic refit it fed — recovery rates, the excluded parent-fallback grants, the awardee-org attribution audit, the `min_cluster_size` sweep, and whether "titles carry most of the signal" still holds. Reads only already-computed parquet/JSON artifacts; executed end-to-end during authoring to verify its own numbers. |

## Topic modeling — state of play

- **The curated keyword classifier (BM25F) is now canonical, replacing BERTopic, as of 2026-08-29.** The full topic-model redo (Plans A/B → Phase 4a curation → Phase 4b classifier → Phase 3 validation → downstream integration) went from "designed, nothing built" to fully implemented and wired into the live pipeline in one session. **31 leaves / 8 parents** (`outputs/topic_keywords.json`, `kw_curation.py --check` passing 0 errors), scored by `src/classify_by_keywords.py` (stdlib/pandas only — no torch/bertopic/umap/hdbscan, so it finally runs fully offline and reproducibly). Output: `data/processed/topic_keyword_assignments.parquet`. `outputs/topic_labels.json` is no longer BERTopic's own seed file — it's now that classifier's `--write-topic-labels` conversion of the curated taxonomy, so `src/build_viz_data.py` (unchanged read contract) now joins the keyword classifier's `kw_leaf_id` as the canonical `dom`/`isNoise` for every downstream consumer (EnricoVis JSON → `build_viz_aggregates.py` → `docs/TopicVizPrototypes/`). **BERTopic's own assignment is kept, not deleted** — `topic_assignments.parquet` stays byte-untouched, joined in as `bertopicDom`/`bertopicNoise` comparison columns on every point, exactly per the redo plan's "kept as a comparison column" decision.
- **Unassigned collapsed from 697 grants / 26.7% of dollars ($582.7M) to 66 grants / 2.5% of grants / 1.9% of dollars ($40.4M)** — by a wide margin the headline result of this redesign. Broken down honestly via a new `unassignedReason` per grant (`coverage.json`'s `unassigned.by_reason`, replacing the old HDBSCAN-noise/artifact-topic split, which no longer means anything under a keyword classifier): **38 `no_keyword_evidence`** (real text, but literally zero curated terms fired — a genuine curation-coverage gap worth a future curation pass, since only 856 of the ~2,506 pruned candidate terms survived Phase 4a curation) and **28 `placeholder_title_only`** (the same ONR "Grant"-titled records BERTopic's old artifact topic 14 held — unclassifiable by any text method, keyword or embedding). `ARTIFACT_TOPIC_ID` is **retired (`None`)** in `src/build_viz_aggregates.py` — every leaf in the curated taxonomy is a deliberate human decision, not an HDBSCAN byproduct, so there's no single hardcoded "flagged bucket" anymore.
- **A real matching bug was found and fixed post-hoc via manual spot review, not by any automated check** — worth internalizing as a lesson, not just a changelog line. `src/keyword_match.py`'s "collapsed" tier stripped ANY intra-token `-./+`, including a TRAILING symbol with nothing to bridge (the curated LIGO-era term `"a+"` collapsed to bare `"a"`), which matched the English article "a" throughout unrelated abstracts — confirmed concretely misrouting an antimicrobial-chemistry grant to "Gravitational Wave Detection & LIGO Instrumentation" at "medium" confidence. Fixed by only collapsing a separator with alphanumeric content on BOTH sides (`(?<=[A-Za-z0-9])[-./+](?=[A-Za-z0-9])`), same as the real bridging cases (`covid-19`→`covid19`, `pm2.5`→`pm25`) but not degenerate trailing/leading symbols. Re-running after the fix **improved every measured number**: parent-level BERTopic agreement rose 67.9%→**75.5%** (into the plan's "healthy" 0.70–0.90 band), low-confidence share dropped 26.6%→**19.9%**, unassigned rose slightly (100→114 grants) as the honest cost of no longer counting a false-positive match as evidence. A follow-up manual review of the 4 unassigned grants that DID have real abstract text found one genuine curation gap (grant 726280, "Visualizing Oral Histories of Bengal using Digital Humanities Tools") — adding `"oral history"` (df_corpus=1) and `"digital humanities"` (df_corpus=3) to Leaf 12 (Social Science & Public Policy) fixed it, and as a bonus corrected a real misclassification (grant 1619895 had been landing in the Predator-Prey ecology leaf via "ecology" used metaphorically, not literally) — down to 73 unassigned / 45 `no_keyword_evidence` after three total manual curation passes on the remaining title-only grants — materials/alloy, cancer-nanoparticle, bone/skeletal, military-funded ML, NOAA fisheries, humanities, traineeship, and network-science clusters (all added as new keywords to their existing best-fit leaf), plus a final pass that RELABELED three leaves as their scope broadened by human decision: Leaf 20 "Gravitational Wave Detection & LIGO Instrumentation" → "...& Observational Astrophysics" (absorbed a 10-grant NASA XMM-Newton/tidal-disruption-event research program), Leaf 23 "5G/mmWave & Cellular Wireless Communications" → "Wireless Communications, Antennas & Electromagnetic Metamaterials" (absorbed 4 ONR/AFRL antenna/metamaterial grants), and Leaf 18 "Algebra, Geometry & Mathematical Physics" → "...& Metascience" (absorbed 2 AFRL scientometrics grants) — one candidate grant (1777815, ambiguous chemistry/quantum-theory titling) was deliberately left Unassigned rather than force-fit. Caught only by manually tracing where specific real grant titles landed and asking "does this make sense" — no automated test in this repo's suite would have caught it, since the bug produced syntactically valid, internally consistent output. A regression test now exists (`tests/test_keyword_match.py::test_collapse_does_not_degenerate_trailing_symbol_to_a_common_word`).
- **A related, separate tokenization limitation was found during the manual curation pass above — NOT fixed, a genuine design tradeoff.** `kw_vocab.tokenize()`'s `KW_TOKEN_PATTERN` keeps a `/` intra-token so real compounds (`i/o`) survive as one token — but a grant title like `"...Dynamical Systems/Machine Learning Approach"` (no spaces around the slash) tokenizes to `["systems/machine", "learning"]`, not `["systems", "machine", "learning"]`, so the already-curated term `"machine learning"` never matches even though it's right there in the text (confirmed: grant 1570983). Fixing this generically would risk under-tokenizing genuine slash-compounds elsewhere — left as a known, narrow gap rather than special-cased.
- **A second real curation bug (distinct from the `"a+"` collapse bug) was found via manual inspection of the embedding-centroid diagnostic's negative-margin cases** — not a code bug this time, a term-choice bug in the ORIGINAL Phase 4a curation (leaf 10, "Groundwater/Soil Chemistry & Water Quality"). The curated term `"wells"` plural-folds to `"well"` at the stem tier, which then matches the extremely common English adverb "well" ("as well," "well-known") anywhere it appears — **42 of that leaf's 86 assigned grants** (an Osteoarthritis study, a Fermi-level condensed-matter-physics grant, a satellite-servicing SBIR — none groundwater-related) were there purely because of this collision, found by tracing exactly the kind of case the plan's embedding-centroid check is meant to surface (large negative `centroid_margin` = keyword pick and embedding geometry disagree). The leaf's other single-word terms (`water`, `oxygen`, `carbon`, `gas`, `ph`, `bubbles`, `saturation`/`saturated` — the latter also collides with vision-science "color saturation") were confirmed generically risky the same way by inspecting real member grants, not guessed at. Fixed by pruning to the 6 terms actually driving this leaf's high-confidence, genuinely on-theme members (`water quality`, `north carolina`, `coal`, `sodium`, `electrolysis`, `soil`) — every one of the 19 misclassified grants checked individually landed correctly afterward (several exactly where the embedding signal had already suggested, e.g. "Towards Transparency of Personalization on the Web" → Theoretical CS/Network/Data Science). **Lesson for any future curation pass: before curating a single-word term, check whether its stem-folded form collides with a common English word, and treat any confidently-single-word term as higher scrutiny than a multi-word phrase** — the two real bugs found this session (`"a+"`→`"a"`, `"wells"`→`"well"`) were both exactly this failure shape.
- **The embedding-centroid independent-signal check (Step 3 validation) now flags a real, unresolved concern, revised after the bug-fix re-run.** The 610 grants BERTopic left as noise-with-text are now **concentrated in 3 parents** (Computing/Networking/Robotics 27.9%, Social Science/Policy/Workforce 24.8%, Biomedical Sciences 22.5% — 75% combined) — the plan's own named "coverage hole just relabeled" failure signature, not the reassuring "spread out, no concentration" result a pre-bug-fix run had shown. Their mean centroid margin (0.0121) is still measurably below confident docs' (0.0262), so the confidence signal itself isn't obviously broken — but the concentration is worth investigating before trusting this population's labels. The title-only normalization check still fails, and by more than first measured (gap widened 12.2pp→**24.9pp** once the bug's false-positive matches — which had been artificially helping some title-only docs look assigned — were removed); `notebooks/09_keyword_classifier_validation.ipynb` and `src/validate_keyword_classifier.py` both carry the corrected numbers (re-run and re-verified after the fix, not left stale).
- **Parent-theme count is now 8, with entirely new names/order** (`PARENT_NAMES` in `src/build_viz_aggregates.py`, byte/value-identical to `docs/TopicVizPrototypes/shared/enrico.js`'s own copy): Biomedical Sciences · Public & Behavioral Health · Environmental Science & Ecology · Social Science, Public Policy & Education Research · Materials Science & Structural/Civil Engineering · Mathematics & Fundamental Physics · Computing, Networking & Robotic Systems · Workforce Development & Institutional Partnerships. The old 8 BERTopic-era names ("Life Sciences & Biomedicine", "Education & Learning", etc.) never matched the curated keyword lists and are gone, not kept as unused history — this 8 is a coincidental same count, entirely different parents. **This went 8→7→8 in one session**: the initial promotion merged workforce/career-pipeline content into "Social Science, Public Policy & Workforce Development" (7 parents), but a follow-up review found Leaf 13 (Faculty Development, Diversity & Institutional Partnerships) was that parent's *largest* leaf by both count (201) and dollars ($234.7M) — bigger than the actual social-science leaf (179/$123.3M) — diluting its identity. Split by what the grants actually *do*, not surface STEM-adjacency: the redefined P3 keeps Leaf 12 (Social Science & Public Policy) + **Leaf 14** (Education, Games & Informal STEM Learning — genuine learning-*science research*, verified via its own keyword content — `science learning`, `pedagogy`, `classroom`, `learning environment` — to be methodologically closer to social science than to career-pipeline administration) + Leaf 15 (Security/Trafficking/Disaster Resilience); new P7 "Workforce Development & Institutional Partnerships" holds Leaf 13 + Leaf 11 (Conference & Student Travel Awards). Leaf count is 31 (unaffected — leaf ids/keyword lists/labels didn't change, only their `parent` field). Both counts stay comfortably inside the frontend's pre-allocated color-palette headroom (`TOPIC_COLORS`: 32 slots; `PARENT_COLORS`: 12 slots). **Gold-set consequence (resolved)**: 29 of the 180 labeled rows had recorded the old combined parent name and needed re-labeling into the two new parents before `validate_keyword_classifier.py`'s `gold_set_report()` could score them again — done; see "Gold-set labeling is DONE" below for the resulting numbers. A real, non-obvious side effect of restructuring parent labels *after* human labeling had already happened; worth remembering before any future parent-level taxonomy change. `what_we_can_see/facets.js` also gained a new "Confidence" facet (ordinal, `none`→`low`→`medium`→`high`) on the Every-Grant grid — a genuinely new capability the old BERTopic one-hot data couldn't support at all (`build_facets()`'s `cols["conf"]`).
- **Secondary-theme signal now exposed** — the classifier already computed a runner-up leaf/score per doc (`kw_leaf2_id`/`score2`/`margin_rel`) but never surfaced it; addresses the "no process for interdisciplinary grants" gap (e.g. HCI, which could legitimately be Computing, Public & Behavioral Health, or Social Science depending on framing). `src/build_viz_data.py` adds `secondaryLeaf`/`secondaryParent`/`secondaryMargin`/`hasSecondaryTheme` per point; `hasSecondaryTheme` is deliberately scoped to CROSS-PARENT runner-ups within a 0.15 relative-margin threshold (not same-parent-different-leaf, which is "which flavor of X," not the interdisciplinary case) — verified against the real corpus: 252 grants (9.4%). Shown as one "Also relevant to: ..." line in the grant detail card (`what_we_can_see/detail.js`), not a new facet/filter — deliberately additive, no new scoring.
- **Phase 4c's trigger criteria widened past low-confidence-only** — `src/adjudicate_low_confidence.py` (still scaffolded, still not run) now also flags docs matching a verified `PEDAGOGY_SIGNAL_TERMS` list (`"undergraduate course"`, `"curriculum development"`, etc.) REGARDLESS of `conf_tier`, targeting the "deceptive framing" gap: a grant whose vocabulary points to one domain but whose true purpose is teaching it, which can happen at HIGH confidence (confirmed: grant 1171382, `high` confidence, flagged only by this new trigger). Widened scope: 641 docs (was 594 low/none-only), +47 from the new trigger — still bounded, not the full corpus.
- **Title-only calibration — RESOLVED (2026-08-30), and the fix was curation, not the BM25F constants.** The 24.9pp title-only-vs-abstract confidence gap this section used to report here was real at the time, but investigating it (before spending the sweep below) found the recorded diagnosis was wrong: **65 of the 66 unassigned grants were title-only**, and excluding unassigned docs entirely, title-only grants were already *better* calibrated than abstract-bearing ones (a −7.5pp gap, the *opposite* direction). So the whole headline gap was really the unassigned-title-only population, not `W_TITLE` over-boosting assigned docs' confidence — plus a second, independent structural cause: `conf_tier == "high"` requires `n_terms_matched >= 3`, but title-only docs average only 1.12 matched terms (vs 5.37 for abstract-bearing) and just 6.5% of them ever reach 3, so they're structurally capped below "high" regardless of margin. **Neither cause is fixable by retuning K1/B/ALPHA/W_TITLE** — no constant value can create a match where zero curated terms fire. A targeted curation pass (`src/kw_review_sheet.py --unassigned no_keyword_evidence`, then 34 new terms added across 12 leaves, verified against real per-term `df_corpus` via the new `scripts/_kw_term_df.py`) closed 30 of the 38 `no_keyword_evidence` grants directly. Result: unassigned fell 66→36, the title-only gap fell **12.1pp→1.0pp** (comfortably inside the ≤10pp target), gold accuracy held flat (68.9%→68.3%, within noise), and BERTopic parent agreement held flat (75.9%→74.7%, still "healthy"). The 8 grants still `no_keyword_evidence` are individually accounted for, not a residual mystery: 6 deliberately left unassigned rather than force-fit (1 genuinely ambiguous chemistry/quantum-theory title — grant 1777815, a prior documented decision — plus an admin-boilerplate NIH subaward, a too-thin program-evaluation title, two grants with no fitting leaf in the current taxonomy, and one near-placeholder "Block Grant" title), and 2 (`nih_reporter_parent` abstract_source, grants 1089127/1250146) are genuinely model-invisible — their abstract text is DISPLAYED on the dashboard but masked to `""` for the classifier by design (caveat #4 below), so no curated term could ever match them regardless of what's added. `src/kw_review_sheet.py`'s `render_unassigned()` now flags this low-trust-source case explicitly so a future curation pass doesn't repeat the mistake of curating from text the model can't see (confirmed the hard way this session).
- **BM25F constant sweep — done, keep the literature defaults.** `src/tune_bm25f.py` (new; match-once/rescore-many over `src.classify_by_keywords`'s new `match_corpus()`/`score_corpus()` split — matching is ~33s and invariant to every constant, so a 108-point grid over K1/B/ALPHA/W_TITLE/HIGH_MIN_TERMS scores in seconds, not hours) swept against the now-labeled gold set, guarded against overfitting an n=180 set: a candidate must beat baseline by more than the baseline's own 95% CI half-width (±6.8pp) to be "adopted," not just edge it out. **Zero of 108 configurations cleared that bar** (best swept accuracy 68.3%, identical to baseline; spread across the whole grid was 66.7%–68.3%, i.e. noise) — recommendation is to keep `K1=1.5`, `B=0.75`, `ALPHA=0.5`, `W_TITLE=2.0` exactly as they are. This is the correct outcome given the finding above, not a failed sweep: the real problem (curation coverage) was already fixed by curation, so there was nothing left for the constants to move.
- **Phase 4c (bounded LLM adjudication of the low-confidence tail) is scaffolded, not run** — `src/adjudicate_low_confidence.py`, scoped to `conf_tier ∈ {low, none}` AND `unassigned_reason != "no_usable_text"` (a few hundred docs), Batches API, structured output with a first-class `abstain`. No `ANTHROPIC_API_KEY` is set in this environment and the sandbox only allowlists `pypi.org`, so — same precedent as the NIH RePORTER/NSF Award Search backfill — this needs the user to run it themselves.
- **BERTopic is now historical/comparison-only, not canonical** (SPECTER2 → UMAP → HDBSCAN → c-TF-IDF, 32 topics + noise, `min_cluster_size=20`, re-fit 2026-08-20). Still fit via `src/topics_bertopic.py`, output still `data/processed/topic_assignments.parquet` — nothing about *producing* that file changed, only its role downstream: it's no longer what `build_viz_data.py`/EnricoVis JSON/`TopicVizPrototypes` render, only a `bertopicDom`/`bertopicNoise` comparison column and the `agrees_with_bertopic`/parent-crosswalk validation signal described above. `ARTIFACT_TOPIC_ID` (the old placeholder-title bucket, id 14) is a **BERTopic-era concept**, still meaningful for interpreting `topic_assignments.parquet` on its own terms but retired from every place that used to compare against it directly (`build_viz_aggregates.py`, `kw_review_sheet.py`). See `docs/TOPIC_WORK_EXECUTION_REPORT.md` (M1–M4) and `docs/TOPIC_MODEL_REFIT_CHECKLIST.md` (now split into a *refit* track for this and a lighter *re-curate/re-score* track for the keyword classifier) for the full BERTopic-era history.
- **LDA is historical, not canonical, and was already superseded before this redo** — `src/topics_lda.py` kept as a labelled legacy module; nb06 still runs standalone for comparison but feeds nothing downstream. BERTopic fixed LDA's cross-vocabulary mis-parenting; the keyword classifier's own agreement-with-BERTopic number (above) is the closer analogue for "is this better," not anything LDA-related.
- **Text cleaning is unified** in `src/clean_text.py` (with a regression test suite), shared across the LDA, BERTopic, AND keyword-classifier paths (`src/model_docs.py`'s `load_doc_fields()` reuses `clean_text.model_doc_halves()` verbatim, so all three can't drift on cleaning/masking).
- **SPECTER2 / UMAP / HDBSCAN CANNOT run in CI** (no HuggingFace network access there) — still true, and now scoped specifically to the BERTopic comparison path; the keyword classifier itself needs none of these. In a *local* dev sandbox, the heavy embedding step runs fine once the model is cached, but must run in the **foreground, unsandboxed** — backgrounding it (even unsandboxed) silently produced no output in this environment. Always precompute locally and commit the cached artifacts (`specter2_embeddings.npy` + `specter2_ids.txt`); CI and the HTML apps consume only committed artifacts/JSON.
- **M5a (NIH RePORTER abstract backfill) is done** — see `docs/TOPIC_WORK_EXECUTION_REPORT.md`; unrelated to the keyword-classifier redo, its recovered text is exactly what the classifier scores.
- **A real-browser pass on the three `TopicVizPrototypes` pages is done and passed** (user-confirmed 2026-08-29, post P3-split/secondary-theme/Phase-4c-widening changes) — `scripts/_check_topicviz.py`'s static checks (now including a PARENT_NAMES/PARENT_COLORS byte-identity check, a palette-capacity check, and a caveat-id-whitelist-vs-`CAVEATS` check) had already passed against the new keyword-classifier output; the live-render check confirms no rendering/layout/interaction regression from that round of changes. Any *future* schema/UI change still needs its own fresh browser check — this doesn't grandfather one in.

## Open threads

- **Calibration + visualization finalization pass — DONE (2026-08-30).** The former "NEXT CONCRETE STEP" (recalibrate the BM25F constants against the gold set) is resolved above — curation, not the constants, was the actual lever; the sweep confirmed the constants themselves need no change. Also completed in the same pass: the topic-keyword-fingerprint view and grant search box (both previously "next-direction, not yet built" — see `TopicVizPrototypes/` entry below), a `src/adjudicate_low_confidence.py` merge-back path (`--merge`, previously unwritten) and a true `--dry-run` mode, and most of the 2026-08-21 "what we can see" PI-feedback items (gridlines, click-outside-closes-dial, PI name + colleges-involved in the grant tooltip, dollar-band breakdown on row/column hover, clearer Sort-by/Color-by labels and tooltips, family-grouped academic-rank coloring, PI dollars-earned-at-NEU, and a "Need a suggestion?" preset picker). Two 2026-08-21 items are explicitly NOT code — moving hosting to Enrico's own GitHub Pages project (blocked on his invite) and the Paolo-meeting email — and one (per-facet attribute filtering) was deliberately deferred as a stretch item since it cuts against the grid's own "nobody's silently dropped" invariant and needs its own design decision, not a quick add. **Phase 4c LLM adjudication — RUN LIVE and WIRED INTO THE DASHBOARD (2026-08-30, same day, later in the session).** The user ran the full 619-doc batch themselves (`claude-sonnet-5`, `output_config: {"effort": "medium"}` — added to `_run_batch_live()` for cost/latency, a short classification task doesn't need the default "high" effort) via the Anthropic Batches API — took ~3 hours (longer than the "most batches finish in under an hour" norm, but Anthropic's own status page and the account's usage-tier limits both ruled out an external cause; no code-side fix was needed, it just finished). Two real bugs were found and fixed in the process, not anticipated when this was scaffolded:
1. **A pyarrow crash writing the results parquet** — `terms_considered` (the LLM's structured-output field) came back as a comma-separated STRING instead of a JSON array in 518 of 619 responses (84%), because the tool definition never set `strict: true`, so the schema's `type: array` was only ever a hint, not enforced. Fixed with `_coerce_terms_considered()` (splits the string back into the individual terms) — no data was lost, and no re-run was needed, since the crash happened AFTER every response was already cached; the fix just replays `--offline` from that cache.
2. **A real bug in `merge_adjudication()`'s resolution rule**, found while implementing a "softer fallback" request. The original rule checked `conf_tier in (high, medium)` FIRST and kept the keyword classifier's answer unconditionally in that case — which completely defeated the pedagogy-signal trigger's purpose (catching a confident-but-wrong keyword match), since a pedagogy-flagged high-confidence doc could never be overridden no matter what the LLM concluded. Fixed: the LLM's non-abstained answer now always wins when it exists (resolves the low-confidence tail AND can override a mis-flagged confident match); only when there's nothing to defer to does the deterministic scorer's own pick apply — including a genuinely low-confidence one, now kept visible via a NEW `keyword_classifier_low_confidence` label rather than silently discarded to Unassigned (the requested "softer fallback": Unassigned should mean "neither method could label this," not "the keyword classifier wasn't confident"). Confirmed against the documented grant 1171382 pedagogy case: keyword classifier said "Wireless Communications" at `high` confidence; the LLM disagreed and moved it to "Theoretical CS, Network & Data Science" — the old buggy rule would have silently kept the wrong answer.

Final `final_source` distribution over all 2,676 grants: `keyword_classifier` 78.3%, `llm_adjudication` 11.4%, `keyword_classifier_low_confidence` 8.9%, `unassigned` 1.3% (back to matching the keyword classifier's own honest floor of 36 grants, not inflated by the merge rule). This is now wired all the way to the dashboard, not just sitting in a parquet — `src/build_viz_data.py` prefers `final_leaf_id`/`final_parent_id` over `kw_leaf_id`/`kw_parent_id` once `--merge` has been run (degrades gracefully to the old behavior if it hasn't), and adds a new `assignmentSource` field (never redefining `conf`/`confTier`, which still describe only the keyword classifier's own BM25F result) surfaced as a new "How this topic was decided" facet plus a flagged tooltip line for anything other than a confident keyword match, plus a new CAVEATS entry explaining the whole hybrid.

**A third, unrelated, pre-existing bug was caught while verifying this wiring**: `grants_umap.json` (Python-side intermediate output, not directly fetched by any current browser page — confirmed via the dataset check, so this never broke anything live) was writing the literal invalid JSON token `NaN` instead of `null` for `unassignedReason` on 2,640 of 2,676 grants — Python's own `json` module reads its own `NaN` output back permissively, which is exactly why a real, spec-compliant parser (Node's `JSON.parse`, confirmed to reject it) was needed to catch it. Root cause: the same `pd.DataFrame(list_of_dicts)` None→NaN coercion already documented in `classify_by_keywords.py`'s own test suite, here reaching a JSON writer instead. Fixed at the source (`pd.isna()` check before assignment) and hardened both JSON writers (`build_viz_data.py`, `build_viz_aggregates.py`) with `allow_nan=False` so this class of bug fails loudly at build time from now on instead of writing invalid JSON silently.

**Still open**: a real-browser check of all of this session's `TopicVizPrototypes` changes is required before publishing (see the standing "no browser in this environment" note) — this round touched d3 layer ordering, a new search input, a new facet, and two new panels, so the prior sign-off does not cover it. Nothing from this session has been committed to git yet either.
- **Communicate the redo's completion back to Enrico (the PI) — done** (user confirmed 2026-08-29). His proposed keyword→classifier architecture is fully built: keyword-list curation was a full manual Phase 4a pass (31 leaves/8 parents), and the topic-to-document link is a deterministic BM25F scorer, not an LLM as his brainstorm first proposed (LLM adjudication is now a scaffolded, off-by-default Phase 4c layer, widened past low-confidence-only — see "Topic modeling — state of play"). `docs/TOPIC_CLASSIFICATION_BRAINSTORM.md` already marks this resolved. Still open, separately: which student/prior work he wants looped in is unnamed — follow up if it becomes relevant.
- **Gold-set labeling is DONE (180/180 rows, including a re-label pass after the P3 split below) — real accuracy numbers now exist.** Keyword classifier: 68.9% (95% CI 61.8–75.2%), vs. BERTopic-equivalent 34.4% on the same rows (translated into the new parent vocabulary via the same majority-vote crosswalk `parent_level_bertopic_agreement` uses) — still a decisive ~2x margin. Confidence tiers calibrate correctly (high 81.2% > low 53.3%, well past the plan's own 5pp bar). The accuracy number itself dropped from an initial 72.8% after the P3 split (below) went 7→8 parents: splitting one category into two finer ones makes exact-match accuracy measurably harder without the classifier getting any worse — expected, not a regression. The 38 `no_keyword_evidence` grants were a concrete, boundable curation-coverage gap — now closed to 8 (see the title-only calibration entry above for the full breakdown of what's left and why); the embedding-centroid concentration finding (610 formerly-noise docs) is now genuinely spread across parents post-split (was "concentrated in ≤3 parents" pre-split — the old apparent concentration was partly an artifact of the since-split combined P3 bucket, not a real coverage hole), re-confirmed after the curation pass too (0.0138 vs 0.0269 mean centroid margin, top parent share 28.5%, no concentration).
- **Investigator/co-PI proposals from the 2026-08-20 backfill were never merged** into `faculty_grants.parquet` — `data/nih_nsf_backfill/investigator_faculty_proposals*.parquet` holds ~1,356 NSF and ~433 NIH proposed links (NIH ones need an `is_neu_org == True` filter first — a third of the raw NIH matches are the prime institution's own PI on a subaward, not an NEU person). Deliberately deferred: adopting them would reorder every funding-credit-model leaderboard. Related and much smaller: the AcAn refreshed abstract export's 3 remaining net-new-recoverable grants (`data/processed/new_abstract_recovery.parquet`) were measured but not adopted either, given the tiny yield.

## `docs/` reference map

- `data_dictionary.md` — per-column reference for raw files + join keys.
- `data_quality_report.md` — flagged issues (NSF/NIH bias, pre-hire attribution, roster gaps, abstract coverage).
- `ID_RECONCILIATION.md` — authoritative account of the 4-ids-per-person problem and the personid bridge.
- `INSIGHTS.md` — narrative findings across all notebooks (local-only, not published).
- `TOPIC_ANALYSIS_COMPENDIUM.md` — definitive LDA parameters, coverage bias, follow-ups.
- `TOPIC_WORK_FORWARD_PLAN.md` — the BERTopic migration + orphan-reconciliation roadmap (M1–M5).
- `TOPIC_WORK_EXECUTION_REPORT.md` — companion to the forward plan; documents what M1–M4 actually shipped (see "Topic modeling — state of play" above for the current summary). M5a done (2026-08-20); M5b–f not started.
- `TOPIC_MODEL_REFIT_CHECKLIST.md` — the runbook for redoing the topic model: full command sequence, exactly which artifacts constitute "a new topic model" to swap in, and what still needs a human afterward (parent-theme curation, the manually-synced `PARENT_NAMES`/`PARENT_COLORS` copies, `CAVEATS` prose) vs. what's now automatic (`src/refresh_topicviz.py`). Also covers the topic/parent-count safety audit (see "Topic modeling — state of play" above): which hardcoded counts are already safe, which one crash risk was found and fixed, the color-palette buffers, and what's left as optional polish rather than a safety gap.
- `TOPIC_CLASSIFICATION_BRAINSTORM.md` — record of the PI's proposed transparent keyword→classifier topic method (a candidate alternative/complement to BERTopic), now updated past the pre-meeting prep stage: the two mechanisms his Slack reply left open have both narrowed after an in-person follow-up — keyword-list extraction/curation resolves to a manual curation pass on BERTopic's existing 32 topic keyword lists (as of the 2026-08-20 refit, was 25; no fresh clustering), and the topic-to-document link resolves to using an LLM against those curated lists (not a hand-coded scoring function or a trained classifier). Neither is a finished spec yet (LLM choice, prompt design, cost/scale, curation procedure all still open) and none of it is acted on in code — this doc tracks the narrowed remaining questions.
- `EnricoVis/` — **a parallel visualization effort by the PI**, not this user's own work (treat as read-only reference unless told otherwise). 3 self-contained interactive HTML apps (`grant_atlas`, `topic_islands`, `topic_hierarchy`) + their SPECTER2 pipeline; `grants_visualization_work_breakdown.md` is the handoff doc. Published copies live in `onlineoutput/` (below).
- `TopicVizPrototypes/` — **the user's own** topic-model visualization prototypes: `topic_flow.html` (funding over time) and `what_we_can_see.html`, a **tabbed** dashboard — now **three** tabs (down from six panels across four tabs, after direct PI feedback to deprecate the Coverage tab and the "Does it matter? What we can't see" tab and reinvest that space): **"Every grant"**, **"Every PI"** (new), and **"What's missing & where it goes"**. Both unit-visualization grids ("Every grant, arranged" and "Every PI, arranged") are built by one shared engine, `createGrid()` in `what_we_can_see/grid.js` — a factory instantiated twice (`data: FACETS`/`GRANT_FACET_DEFS` vs. `data: FACETS_PI`/`PI_FACET_DEFS`) rather than duplicated, so every layout/interaction fix applies to both. "Every grant" arranges/splits/sorts/colors by any of 8 facets (agency, year, college, NEU attribution, has abstract, parent theme, leaf topic, dollar band); "Every PI" does the same over 11 facets (college, department, rank, appointment track, tenure status, hire year, employment status, has-grants, grant-count band, dollars-as-PI, parent theme-as-PI) across **all 2,247 roster faculty**, not just the 570 with a grant in this corpus — "no grants" is a first-class bin, same "nobody's silently dropped" invariant `facets.json` already holds for grants. Dollars/theme on the PI grid are credited **PI-only** (not full- or fractional-credit), per the funding-credit-model caveat. A fixed 4.8px mark size applies to every Rows×Columns arrangement (previously reflowed between five size tiers, which PI feedback flagged as distracting); the controls dock is a floating, default-collapsed overlay, anchored under a `.gridtoolbar` row (not the whole section, as before) so it always opens directly beneath its own dial regardless of title length; clicking a mark or cell selects it via nearest-hit-testing and opens a floating "Selected" card (not a modal — one was built and explicitly rejected earlier), `position:absolute`, anchored to the top-right corner of its own chart canvas (`#facetchartwrap`/`#pichartwrap`) — an earlier `position:fixed`-to-viewport design was replaced after feedback that it could land over the section's own toolbar/legend row depending on scroll position, trading away staying on screen through a very tall chart's scroll for reliably sitting at the canvas's own top-right — with its own close button and Esc-to-dismiss. The Selected-PI card's grant list shows the first 5 titles automatically, with any remainder (up to 8, the aggregator's own per-PI cap) behind a "Show N more" native `<details>` disclosure that scrolls itself into view on open. Both grid headers dropped their "How this is computed" drawer and description line (PI feedback: unnecessary, took up space), and — in a later session — their caveats row too (moved to a new `about.html` page, see below): each header is now just a title and a single "dial" toggle, no caveats row anymore. The dial replaces the shared kit's two-button fold/opener pattern (`shared/enrico.css`'s `.dock`/`.opener`, still used as-is by `topic_flow.html`) with one persistent toggle (`setupDial` in `what_we_can_see/grid.js`). It was first pinned to the section's true left margin deliberately NOT next to the title text (a follow-up correction at the time) — then reversed again later: dial and title now sit together in a `.gridtoolbar` row (dial closest to the true left margin, title right after), because collapsing the row-label lane to 0px whenever Rows is "— none —" (see below) left the old floating dial covering the chart's own column headers. The color legend moved the same way — out of a floating overlay over the chart's corner into the same toolbar (right side: legend closest to center, its own open/close toggle closest to the true right margin) — defaulting OPEN (unlike the dial) and becoming a floating overlay itself only once its content would actually need a vertical scrollbar (`measureLegendOverlay`, ~56px threshold), so a legend that fits inline never grows the toolbar. The dock's four cards are now ordered Columns → Rows → Color by → Sort bins (was Rows → Columns → Sort → Color), and Rows can be set to "— none —" too (previously only Columns could) — the default arrangement is column-first (Columns = the facet, Rows = none), not row-first. Row/column labels also abbreviate long college names at render time (`COLLEGE_SHORT`), falling back to the full name on hover. "What's missing & where it goes" now covers three grains via a Grants/PIs/Abstract-records switch (each scored against its own population — 2,676 grants, 2,247 roster faculty, 8,075 raw abstract-upload records), a sortable field-by-field table (Known/Missing/%/Recoverable/Where the gap comes from), the by-agency and by-year coverage bar charts salvaged from the deprecated Coverage tab's heatmap, the kept NIH-vs-NSF cliff chart, the mosaic panel's finding as one line, and the unchanged abstract-sourcing funnel; the "What we cannot see" negative-space card deck was dropped (its points now live in `viz_meta.json`'s `caveats[]`, several of which weren't rendered anywhere before). Reads EnricoVis's canonical BERTopic/SPECTER2 output as a read-only upstream input (`docs/EnricoVis/data/{grants_umap,topics}.json`) via `src/build_viz_aggregates.py`, which emits `viz_meta.json`/`topic_time.json`/`coverage.json`/`facets.json`/**`facets_pi.json`** (new, over `data/processed/faculty.parquet` + `faculty_id_lookup.parquet` + `faculty_grants.parquet`)/`missingness.json` (now `{"grains": {"grants":..., "pis":..., "abstract_records":...}}`, each field carrying a `where` string and an optional `recoverable` count)/`funnel.json`; writes only into its own `data/` and `shared/`. A new diagnostic, `scripts/_check_new_abstracts.py`, cross-checks a refreshed abstract export the data team dropped (`DataSet/AcAn Grants 2026-08-13.xlsx`) against the pipeline's current one and writes `data/processed/new_abstract_recovery.parquet` (not adopted into `build_dataset.py` itself — see `docs/data_quality_report.md` §9 and `docs/TOPIC_CLASSIFICATION_BRAINSTORM.md` for why: it would desync the recovered text from the frozen, unrefit BERTopic output — newly-recovered abstracts get no real topic assignment until the model is actually refit, see `docs/TOPIC_MODEL_REFIT_CHECKLIST.md`. `build_viz_aggregates.py`'s `validate()` no longer hard-blocks on a corpus-size change on its own, since a later session softened its literal asserts to informational lines, but that was never the harder half of the problem). **House convention: all audience-facing copy (section headers, "How this is computed" drawers) is plain-language only** — no file/column/function names (`.parquet`, `grant_id`, etc.); that detail belongs in this repo's own docs, not the dashboard. **Publishing decided:** the user confirmed all of `TopicVizPrototypes/` should be online, not just `what_we_can_see.html` — all three prototype HTML files are wired into `.github/workflows/deploy-notebooks.yml`. A new third page, `about.html`, holds everything the caveats-row removal above took off the two grid pages: the coverage headline, all of `VIZ_META.caveats[]` grouped by severity, and the model's `frozen_inputs` summary — linked from both prototypes' headers and wired into the same deploy step as the other two. **Hover-tooltip redesign:** row/column axis labels now show the aggregate group detail (count, dollars, color breakdown, merged across every cell sharing that row's/column's axis index); hovering inside the grid itself shows only the hovered mark's own individual-record detail — previously both tooltips existed and which one appeared depended on exact pointer position within a cell, which read as confusing. **PI parent-theme coloring:** the Every-PI grid's "Parent theme (as PI)" facet has two different no-data bins — "No grants as PI" and "Unassigned" — that used to share one grey; "Unassigned" is now `#9AA0A6` (a darker grey already used elsewhere in this file, e.g. the status facet's "Departed"), keeping the lighter `NOISE` grey (`#c7ccd3`) for "No grants as PI".

**`about.html` merged back into `what_we_can_see.html` (2026-08-30) — the standalone third page described in the two paragraphs above no longer exists.** Its content (coverage headline, every `VIZ_META.caveats[]` grouped by severity, the `frozen_inputs` model summary) is now the first section of the "What's missing & where it goes" tab, renamed **"About this data & what's missing"** (`missingfunnelpanel` → `#aboutsection` + the pre-existing `#missingsection`/`#coveragesection`/`#funnelsection`, all still one tab, one hidden panel). `docs/TopicVizPrototypes/` is down to **two** HTML pages (`what_we_can_see.html`, `topic_flow.html`), not three — `about/data.js` is gone too (both its datasets, `VIZ_META`/`COVERAGE`, were already a subset of what the tab's own `data.js` fetches, so no new data wiring was needed). The tab's own `key: "missing"` is unchanged (only the label changed), which mattered because `about.html`'s own footer already linked to `what_we_can_see.html#missing` before the merge — `setupTabs`'s existing hash-routing (`shared/enrico.js`) picks it up with no new JS. Both former header links to `about.html` (on `what_we_can_see.html` itself and on `topic_flow.html`) now point at `#missing` / `what_we_can_see.html#missing` respectively. `.github/workflows/deploy-notebooks.yml` and `scripts/_check_topicviz.py`'s `PAGES` list were both updated to stop treating `about.html`/`about/` as a fourth artifact.

**About section showcase redesign (2026-08-30, later same day) — the merged-in About section's two main showcases were rebuilt from weak bar-lists into real charts, backed by a new backend aggregation.** `src/build_viz_aggregates.py` gained `build_college_collab()`, emitted as `VIZ_META.college_collab` (`n_cross_college`, `dollars`, `pairs[]`, `by_college[]`, `by_year[]`) — sitting on a corrected `load_colleges_per_grant()` that now applies the same `COLLEGE_NORMALIZE`/`COLLEGE_POOL_TO_OTHER` step `college_idx()` already used (previously raw, unnormalized college strings had silently inflated the shipped cross-college grant count by 22%, 128 vs. the real 100). On the frontend, `what_we_can_see/about.js` replaced a dollars-only "by agency" bar list with an **agency dumbbell chart** (share of grant count vs. share of dollars on one axis, never dual-axis — surfaces that NSF has 3x NIH's grant count but only 1.2x its dollars, since NIH awards run ~2.2x larger) and a college-pair bar list (two full college names crammed into a 230px ellipsized label) with a **full 10×10 college-collaboration matrix heatmap** — every college is both a row and a column, mirrored across a distinct grey diagonal (within-college collaboration, genuinely never computed by `build_college_collab()`), with a full `.cellgrid` outline under every cell so a row/column can be traced even through never-co-occurring pairs. Both charts are directly modeled on `notebooks/05_collaboration_network.ipynb`'s own established forms (cell 17, `w7_cross_college_matrix.png`), found via an Explore-agent survey of that notebook rather than designed from scratch. The matrix's cell size is computed so its total width is always ≥720px, matching `.aboutarticle`'s own established prose reading measure, rather than a fixed pixel guess. New gotcha found in the process: a chart's own container needs a JS-set `max-width` separate from shrinking its SVG viewBox — `.aboutstage svg{width:100%}` (deliberately shared with the dumbbell chart, which SHOULD stretch full-width) will always stretch a compact viewBox back up to fill its parent column otherwise, so shrinking cell size alone does nothing visually.

**Structure refactor (source readability, no behavior change):** `what_we_can_see.html` had grown to 5.7 MB (one line, `FACETS`, alone was 5.3 MB) by inlining its six datasets as `const NAME = {...};` blobs, duplicating `data/*.json` inside the HTML — 25% of `.git`'s pack size came from that one file's history. Since these pages are only ever viewed over a local HTTP server or GitHub Pages, never `file://`, all three pages now `fetch()` `data/*.json` at load from ES modules instead: `data/` is the single source of truth, with no duplicated copy in the HTML (`what_we_can_see.html` is now ~11 KB). `what_we_can_see.html`'s inline `<style>`/`<script>` were also split into `what_we_can_see/{style.css, data.js, constants.js, facets.js, layout.js, detail.js, grid.js, missing.js, main.js}` (`grid.js` — `createGrid()` — is deliberately kept as one ~360-line file: its per-instance closure state can't safely be split across the two live grid instances); `topic_flow.html`/`about.html` got matching `topic_flow/data.js`/`about/data.js`. Each page's `data.js` exports a `DATASETS` map, which `scripts/_check_topicviz.py` (replacing the deleted `scripts/_inline_topicviz_data.py`) regexes out to verify every fetched dataset exists in `data/` and parses, and that `build_viz_aggregates.py` emits nothing no page reads — that script also runs a JS import-graph/named-export/cycle check and an id cross-reference, and is wired into `src/refresh_topicviz.py` as its (now read-only, not mutating) final step. `.github/workflows/deploy-notebooks.yml` copies `data/`, `shared/`, and each page's own module directory into `docs/onlineoutput/` at the same relative paths (untracked, CI-only — guarded by `.gitignore`). **Still open, blocked on the PI:** a topic-reliability/manual-inspection panel — he has now replied on the classification method (confirming the *architecture*: a transparent classifier where topics are defined by human-inspectable keyword lists, and documents are linked to topics through those lists) but left the two concrete mechanisms — how the keyword list per topic is extracted/curated, and what function links topics to documents via keywords — explicitly unresolved pending a brainstorming session; he also still wants a student from related prior work looped in first (not yet named). **Next-direction priority — BOTH BUILT (2026-08-30).** A grant search box (`what_we_can_see/main.js`'s `wireSuggestions`-adjacent search wiring + `grid.js`'s `matchesSearch`/`markOpacity` — highlights matching marks by title/PI/agency in place, dims non-matches, never removes any from the grid) and a topic-keyword "fingerprint" view (`detail.js`'s `highlightMatches`/`grantDetail` — the selected grant's abstract with its classifier-recorded matched terms highlighted in place, using the classifier's own recorded matches, not a client-side re-score) are both implemented — a working prototype of the still-open topic-reliability/manual-inspection panel above. The earlier-planned "Round 2" (money-vs-volume slope chart, treemap, agency→theme→college Sankey) remains further back in the queue and hasn't been picked back up.
- `TopicVizPrototypes/README.md` — how to actually run the three prototype pages: one-time `uv venv --python 3.11 .venv` + `uv pip install -r requirements-viz.txt` setup (bare `python3.11 -m venv` fails on this machine), refreshing `data/*.json` via `python -m src.refresh_topicviz`, and serving locally (`python -m http.server 8000 --directory docs/TopicVizPrototypes` — the pages fetch() ES modules and do not work over `file://`).
- `onlineoutput/` — the actual published site (nbconverted notebooks + EnricoVis apps + TopicVizPrototypes apps + index.html); committed to git despite being CI build output.
- **Deprecated:** `WEEKLY_PLAN.md` (the 13-week plan and Dash kiosk/browser delivery target it described are superseded — see "What this project is" above) and `07_grant_projection_specter2.html` (nb07's interactive BERTopic projection, written to `docs/` root; superseded, not copied into `onlineoutput/` by the deploy workflow).
- **Stale / secondary — do not treat as current:** `SETUP_GUIDE_WEEK3_OLD.md` (old table names/counts), `PUBLISHING.md` (describes an old `docs/index.html` / `--output-dir=docs` setup the workflow no longer uses — actual output goes to `onlineoutput/`), and `NedaNotebooks/` (a parallel EDA track with a different conda env and different numbers — `Capstone_Report_Jun_2 (1).pdf` lives here too, as parallel-work supporting material, not the canonical pipeline's report).

## Conventions & gotchas

- **Never commit notebook outputs.** Use `nbstripout` (in requirements) or "Restart kernel & clear outputs" before `git add`. Notebooks are committed with outputs stripped; the CI executes/renders them.
- **`data/processed/` and `outputs/` are gitignored** (`*.parquet` too), with two targeted exceptions: `outputs/topic_labels.json` (the curated topic-label/parent-theme map) is un-ignored via `!outputs/topic_labels.json`, and — added 2026-08-20 — `data/nih_nsf_backfill/*.parquet` (the NIH RePORTER / NSF Award Search backfill's raw caches + derived tables, deliberately tracked so the rate-limited live fetch never has to be re-run) is un-ignored via `!data/nih_nsf_backfill/` + `!data/nih_nsf_backfill/*.parquet`. Note a bare `outputs/`/`data/nih_nsf_backfill/` directory-level ignore can't be selectively un-ignored by a file-level negation at all; the *parent pattern* has to already be a wildcard-on-contents (`outputs/*`, or here just the blanket `*.parquet` rule) for a file-level `!` to work at all — git can't re-include a path whose parent directory itself is what's excluded. Regenerate everything else with `build_dataset.py`; don't hand-edit parquets.
- **A bare directory-name `.gitignore` entry (`lib/`, `build/`, `dist/`, `var/`, `target/`, `parts/`, `env/`, `venv/`, `instance/`, `cover/` — all present in this repo's `.gitignore`, from the Python-packaging template) matches at ANY depth**, not just the repo root — e.g. a future `docs/TopicVizPrototypes/lib/` would be silently untracked. Before adding a new directory anywhere under `docs/`, check `git check-ignore -v <path>` first; this is exactly what protects `docs/onlineoutput/{data,shared,what_we_can_see,topic_flow,about}/` from ever being committed as duplicated CI-only copies (see the `TopicVizPrototypes/` entry above).
- **Caveat — raw `DataSet/*.xlsx` ARE committed** despite `DataSet/ReadMe.md` and `WEEKLY_PLAN.md` stating raw data should not be. Treat the committed raw files as sensitive; confirm before adding/removing them.
- `scripts/_*.py` are **ad-hoc diagnostics**, not part of the pipeline — but note `_diagnose_orphans.py` and `_orphan_faculty_overlap.py` still **write CSVs into `data/processed/`**, so they aren't purely read-only. The authoritative `personid_to_faculty` comes from `build_dataset.py`, not the looser CSV of the same name from `_orphan_faculty_overlap.py`. `_check_new_abstracts.py` similarly writes `new_abstract_recovery.parquet` into `data/processed/` — an optional input `build_viz_aggregates.py` degrades honestly without (see `docs/data_quality_report.md` §9).
- Several legacy parquets (`grant_faculty`, `grant_text`, `faculty_id_lookup`) still sit on disk from older pipeline versions; the current canonical tables are the 7 listed above (see the "renamed outputs" note in `src/README.md`).
- Join keys are frequently coerced to `str` before merging — do the same to avoid dtype-mismatch silent empty joins.
- Code-quality tools available: `ruff`, `black`, `nbstripout`.
- `src/build_viz_aggregates.py` had three now-fixed bugs worth not reintroducing: the "Other" agency bucket was labeled using the first point's `agencyLabel` (made it look like all "Other" grants were USDA); `viz_meta.json`/`coverage.json` each had a field named `share` meaning different things (dollar-share vs. count-share) — now `unassigned_share_d` / `unassigned.share_n` respectively; and the `abstract_records` missingness grain briefly had a `has_text` field (known only among the 5,095 unmatched-only orphan records, `na` for matched ones) that read as a smaller, contradicting count of the grants-grain's own `Abstract text` field (1,936 known) despite scoring an entirely different population — removed rather than re-captioned, since the funnel section already tells that narrower story more precisely.
- D3/SVG panels: a fixed-CSS-height container paired with a dynamically-sized `viewBox` silently scales all content down as it grows (e.g. more facet-grid bins than fit on screen). Size the container to content (with a sensible floor), not a fixed `vh`.
- `.rowlabel` is a class name shared across the Every grant/Every PI facet grids' row labels and other charts (coverage-by-agency bars, the NIH-vs-NSF cliff chart) in `what_we_can_see.html` — only the facet grids' own row labels need `pointer-events:auto` (scoped via `#facetlabels .rowlabel`/`#pilabels .rowlabel`), so keep future `.rowlabel` changes scoped by id rather than applied to the bare class.
- **When splitting/moving existing code across files, always pull the exact original text (e.g. `git show HEAD:path`) rather than reconstructing a block from inference/pattern-matching** — even when the pattern looks obvious. During the `what_we_can_see.html` module split, `PI_FACET_DEFS.tp` was rewritten from inference instead of read from source: wrong value space, would have silently dropped 46 faculty and crashed on first use (selecting that facet). Caught only by an independent review diff against git history — no static check would have found it, since the fabricated version was syntactically valid and internally consistent.
- **Redefining an existing field's meaning is riskier than it looks — add a new field instead.** During the 2026-08-20 backfill work, `build_viz_data.py`'s `titleOnly` was changed from "does NEU have abstract text" to "did the topic model see abstract text" for 5 grants with borrowed, excluded-from-fit text. That single redefinition cascaded into a `build_viz_aggregates.py` internal-reconciliation assertion (compares `titleOnly`-derived counts against an independently-sourced count that assumed the old meaning), a wrong funnel total, and a display desync in the PI's own EnricoVis apps (which would have shown real text as "title only"). Fixed by reverting `titleOnly` to its original meaning and adding a separate `modelTitleOnly` field for the one place that genuinely needed the new meaning (the abstract-vs-assignment-confidence crosstab) — same lesson as `PI_FACET_DEFS.tp` above: a change that looks locally correct can be silently wrong for every caller that assumed the old contract.
- **NIH RePORTER / NSF Award Search API integration gotchas** (from building `src/backfill_nih_reporter.py` / `src/backfill_nsf_awards.py`): NSF's `awardeeName` search parameter silently **ignores** an unquoted multi-word value — no error, it just returns unfiltered nationwide results for the date range — and must be wrapped in literal double quotes (`"Northeastern University"`) to actually filter; caught only by inspecting a `--limit` smoke test's cached raw records before the full live run, not from any error message. Separately, NSF's `coPDPI` field's real live format (`"First Last email@domain"`, sometimes with a `", Jr."` suffix or a `"(Former)"` annotation) never matched the "Last, First" shape guessed from the (JS-rendered, unreadable) written API docs — always verify a guessed external API field format against one real live response before trusting it, especially when the written docs can't be read directly.
- **d3's keyed `enter().append()` inserts a new node at the end of its parent when nothing follows it in that selection** — joining several different-kind sections (headers/cells/marks/selection-ring) directly onto one shared parent `<g>` works on the *first* render only; once any section's join set changes shape (e.g. Rows switched from "— none —" to a multi-level facet), newly-entering nodes from an earlier section land *after* nodes from a later section that already existed, breaking assumed paint/hit-test order. This silently broke mark hover tooltips for every row past the first once Rows held a real facet (`rect.binhit`'s `fill:transparent` still captures pointer events, unlike `fill:none`, so the mis-ordered hit-rect swallowed `mousemove` before it reached the mark underneath — clicking still worked throughout, since it has its own handler, which was the tell). Fixed in `grid.js`'s `createGrid()` by giving each section a persistent, fixed-order layer `<g>` (`hdrLayer`/`emptyLayer`/`cellLayer`/`markLayer`/`ringLayer`) created once and reused every render, rather than joining straight onto the shared `plotroot` — don't flatten this back to one parent.
- **A real browser IS usable in this working environment, via headless Chrome + the DevTools Protocol — corrected 2026-08-30, superseding the earlier "no browser available" claim.** The OS's installed `Google Chrome.app` can run headless with `--remote-debugging-port=<port>`, exposing a CDP JSON API (`http://localhost:<port>/json/new`, `/json/version`, `/json/close/<id>`) that a plain Node script can drive directly with native `fetch`/`WebSocket` — no `puppeteer`/`playwright` install needed (neither is present in this repo, and npm install may not even be reachable offline). This gives REAL verification, not just static structure checks: `Runtime.enable` + listening for `Runtime.consoleAPICalled`/`Runtime.exceptionThrown` catches actual console errors; `Page.captureScreenshot` (or the simpler `--headless --screenshot=out.png --window-size=W,H` CLI flag) gives a real rendered screenshot to inspect; `Runtime.evaluate` can dispatch real `MouseEvent`s (click/mousemove) at real element bounding-rects and read back resulting DOM state (e.g. confirming a tooltip's `classList`/`innerHTML` after a hover), which confirms interaction actually fires, not just that a handler is attached. Always serve the directory first (`python -m http.server 8000 --directory docs/TopicVizPrototypes`; the pages `fetch()` their data and do NOT work over `file://`), then point the headless browser at `http://localhost:<port>/...`. `scripts/_check_topicviz.py` remains valuable as a FASTER first pass (`HTMLParser` tag-balance, re-inlining regression guard, `node --input-type=module --check` per file — bare `node --check somefile.js` silently exits 0 on broken module syntax, confirmed on node v22.14.0, always read from stdin with `--input-type=module` — a per-page id cross-reference, and the dataset-wiring reconcile via `--data-only`), and still can't see an `await`-ordering race (a render running before its `fetch()` resolves) — but it is no longer the ONLY verification available, and a real-browser pass (screenshot + console capture, at minimum) should actually be run before claiming a visual/interaction change is fixed, not deferred to the user with a disclaimer.

## Tech stack

Python 3.11+ · pandas / numpy / pyarrow · matplotlib / seaborn / plotly · scikit-learn / gensim / nltk / wordcloud (topic modeling) · networkx / python-louvain (network) · rapidfuzz (fuzzy matching) · requests (NIH RePORTER / NSF Award Search backfill) · umap-learn + `allenai/specter2_base` (embeddings, local-only) · jupyterlab. Target app framework: Plotly Dash + dash-cytoscape.
