[pytest]
# Put the repo root on sys.path so tests can `from src.clean_text import ...`.
# Without this, bare `pytest` only adds the test file's own dir (tests/) to the
# path and `import src` fails with "No module named src".
pythonpath = .
testpaths = tests
