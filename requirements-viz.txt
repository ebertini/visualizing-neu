# Minimal deps for the ETL pipeline (build_dataset.py, reconcile_orphans.py)
# and viz-data aggregation (build_viz_aggregates.py) — NOT the full
# requirements.txt, which also pulls torch/transformers/bertopic (~2GB) for
# the SPECTER2/BERTopic stack that these scripts don't need.
#
# Install with: .venv/bin/python -m pip install -r requirements-viz.txt

pandas>=2.2
numpy>=1.26
pyarrow>=15.0
openpyxl>=3.1
rapidfuzz>=3.6
python-dateutil>=2.9
