# Reusable code package

Modules added during the project:
- `ingest.py` — load raw xlsx files (Week 2+)
- `clean.py` — normalization / dtype coercion (Week 2+)
- `build_dataset.py` — end-to-end pipeline raw → parquet (Week 3)
